apiVersion: apps/v1
kind: Deployment

metadata:
  name: rq-scheduler
  namespace: {{ .Release.Namespace }}

  labels:
    {{- include "status-page.labels" . | nindent 4 }}

spec:
  replicas: {{ .Values.scheduler.replicaCount }}

  selector:
    matchLabels:
      {{- include "status-page.schedulerSelectorLabels" . | nindent 6 }}

  template:
    metadata:
      labels:
        {{- include "status-page.schedulerSelectorLabels" . | nindent 8 }}

    spec:
      containers:
        - name: rq-scheduler
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          envFrom:
            - secretRef:
                name: {{ .Values.existingSecret }}

          {{- if .Values.redisHA.enabled }}
          env:
            - name: REDIS_SENTINELS
              value: {{ .Values.redisHA.sentinels | quote }}

            - name: REDIS_SENTINEL_SERVICE
              value: {{ .Values.redisHA.masterSet | quote }}

            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.redisHA.authSecret | quote }}
                  key: {{ .Values.redisHA.authSecretKey | quote }}
          {{- end }}

          command:
            - python
            - manage.py
            - rqscheduler

          resources:
            {{- toYaml .Values.scheduler.resources | nindent 12 }}
